/**
 * Enum representing the roles available in the system.
 * - PROVIDER: A user who provides products or services.
 * - EDITOR: A user who edits or manages content.
 */
export enum Role {
  PROVIDER = 'PROVIDER',
  EDITOR = 'EDITOR',
}
