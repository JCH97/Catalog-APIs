// Simple helper for Optional values.

/**
 * Represents an optional value (may be null).
 * @template T
 * @param {T|null} v - The value to wrap as optional.
 * @returns {T|null} The same value, possibly null.
 */
export const Optional = (v) => v;
